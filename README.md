# utrecht.nl dependencies

Gebruik `npm ci` om de dependencies te installeren, zoals de iconenset van het Utrecht Design System.

## Updates installeren

Gebruik `npm run update-dependencies` om de laatste versies te installeren.
